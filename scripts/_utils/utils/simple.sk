
# Pass
# Does nothing, useful for placeholders.
effect:
    patterns:
        pass
        do nothing
    trigger:
        exit

expression colon:
    get:
        return ":"

function str(val:object) :: string:
    return "%{_val}%"
